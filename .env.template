# HF_TOKEN required for OmniVoice / higgs-audio-v2-tokenizer downloads
# Free tier at https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# Path to VoiceStudio's existing .venv (with torch 2.11+cu128 + omnivoice installed).
# Default: ../VoiceStudio/.venv relative to this package
VOICESTUDIO_VENV=/home/jagones/Repositories/VoiceStudio/.venv

# GPU device (single-GPU DGX: 0)
CUDA_VISIBLE_DEVICES=0
